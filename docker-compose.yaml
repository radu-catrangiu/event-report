version: '3.1'

services:
    frontend:
        build:
            context: ./frontend/
            dockerfile: Dockerfile
        container_name: frontend
        networks:
            - net
        environment: 
            NODE_ENV: "production"
        ports:
            - 8080:80
    backend:
        build: 
            context: ./backend/
            dockerfile: Dockerfile
        container_name: backend
        networks:
            - net
    auth:
        build:
            context: ./auth/
            dockerfile: Dockerfile
        container_name: auth
        networks:
            - net

networks:
    net:
